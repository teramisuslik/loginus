# Loginus OAuth 2.0 API - Документация

Добро пожаловать в документацию Loginus OAuth 2.0 API! Эта документация поможет вам интегрировать ваш сервис с Loginus для единой точки входа и управления пользователями.

---

## 📚 Документация

### Для быстрого старта
👉 **[Быстрый старт](./LOGINUS_OAUTH_QUICK_START.md)** - Начните интеграцию за 5 минут

### Полная документация
👉 **[Руководство по интеграции](./LOGINUS_OAUTH_INTEGRATION_GUIDE.md)** - Полное руководство со всеми деталями

---

## 🎯 Что вы получите

При интеграции с Loginus ваш сервис получает:

### ✅ Единая точка входа
- Пользователи могут войти через Email, GitHub или Telegram
- Автоматическая регистрация новых пользователей
- Единый аккаунт для всех сервисов

### ✅ Полная информация о пользователе
- Базовые данные (email, имя, телефон)
- Организации с ролями и правами
- Команды с ролями и правами
- Глобальные роли и права
- Все уникальные права пользователя

### ✅ Гибкая система прав
- Права на уровне организации
- Права на уровне команды
- Глобальные права
- Удобная проверка прав через API

---

## 🚀 Быстрый пример

```javascript
// 1. Перенаправьте пользователя
window.location.href = `https://vselena.ldmco.ru/api/oauth/authorize?` +
  `client_id=${CLIENT_ID}&` +
  `redirect_uri=${REDIRECT_URI}&` +
  `response_type=code&` +
  `scope=openid email profile&` +
  `state=${RANDOM_STATE}`;

// 2. На callback странице получите code
const code = new URLSearchParams(window.location.search).get('code');

// 3. Обменяйте code на токен (на сервере!)
const tokens = await exchangeCodeForToken(code);

// 4. Получите информацию о пользователе
const user = await getUserInfo(tokens.access_token);

// 5. Используйте данные
console.log(user.email);              // Email пользователя
console.log(user.organizations);       // Организации
console.log(user.allPermissions);     // Все права
```

---

## 📡 API Endpoints

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/oauth/authorize` | GET | Инициация OAuth flow |
| `/oauth/token` | POST | Обмен authorization code на access token |
| `/oauth/userinfo` | GET | Получение полной информации о пользователе |
| `/oauth/logout` | POST | Выход из системы |
| `/oauth/clients/register` | POST | Регистрация нового OAuth клиента (требуются права админа) |

**Base URL:** `https://vselena.ldmco.ru/api`

---

## 👤 Данные о пользователе

API `/oauth/userinfo` возвращает расширенную информацию:

### Базовые данные
```json
{
  "id": "uuid",
  "email": "user@example.com",
  "firstName": "Иван",
  "lastName": "Иванов",
  "phone": "+79991234567",
  "isVerified": true
}
```

### Организации
```json
{
  "organizations": [
    {
      "id": "org-id",
      "name": "Acme Corp",
      "role": {
        "name": "Admin",
        "permissions": [
          {"name": "users.create", "resource": "users", "action": "create"},
          {"name": "knowledge.categories.read", "resource": "knowledge.categories", "action": "read"}
        ]
      }
    }
  ]
}
```

### Команды
```json
{
  "teams": [
    {
      "id": "team-id",
      "name": "Development Team",
      "organizationId": "org-id",
      "role": {
        "name": "Developer",
        "permissions": [...]
      }
    }
  ]
}
```

### Все права
```json
{
  "allPermissions": [
    {"name": "users.create", "resource": "users", "action": "create"},
    {"name": "knowledge.categories.read", "resource": "knowledge.categories", "action": "read"}
  ]
}
```

---

## 🔐 Безопасность

- ✅ HTTPS обязателен в production
- ✅ `client_secret` только на сервере
- ✅ Всегда используйте `state` для CSRF защиты
- ✅ Access token действителен 1 час
- ✅ Authorization code действителен 10 минут

---

## 📖 Примеры кода

### JavaScript/TypeScript
См. [Руководство по интеграции](./LOGINUS_OAUTH_INTEGRATION_GUIDE.md#примеры-интеграции)

### Python
См. [Руководство по интеграции](./LOGINUS_OAUTH_INTEGRATION_GUIDE.md#примеры-интеграции)

---

## 🆘 Поддержка

Для регистрации OAuth клиента или получения помощи:
- Обратитесь к администратору Loginus
- Email: [указать email]
- Документация: [ссылка]

---

## 📝 Версии

**Текущая версия:** 2.0

### Что нового в версии 2.0
- ✅ Полная информация о пользователе (организации, команды, роли, права)
- ✅ Расширенный endpoint `/oauth/userinfo`
- ✅ Метаданные OAuth провайдеров и мессенджеров
- ✅ Улучшенная обработка неавторизованных пользователей

---

**Последнее обновление:** 6 ноября 2025

